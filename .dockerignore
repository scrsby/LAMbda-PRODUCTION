# Dependencies – reinstalled inside the image
node_modules

# Build artefacts – produced inside the image
dist
client/dist


# Environment files – injected at runtime via ECS task definition or Secrets Manager
.env
.env.*

# Development / tooling files
.gitignore
README.md
tasks.md
*.log
report.md